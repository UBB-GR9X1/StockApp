<Page
    x:Class="StocksApp.ProfilePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StocksApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Padding="50" Spacing="30" Background="#D6EFF8" BorderBrush="#02367B" BorderThickness="10">
            <!-- Header Section -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="150" Background="White" Padding="20">
                <Button Name="backButton" Content="Back" Click="goBack" Background="#A39FE1" CornerRadius="10" Foreground="White" Padding="10" />
                <TextBlock Text="Profile" FontSize="24" FontWeight="Bold" Foreground="#02367B"/>
                <Button Name="updateWindowButton" Content="Update Profile" Click="GoToUpdatePage" Background="#02367B" CornerRadius="10" Foreground="White" Padding="10" />
            </StackPanel>

            <!-- Profile Info -->
        <StackPanel  Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" BorderThickness="4" BorderBrush="#02367B" Spacing="20" Padding="20" Background="#8fd3fe" CornerRadius="10">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Spacing="15">
                    <TextBlock Text="Image:" FontSize="16" Foreground="#02367B"/>
                    <TextBlock x:Name="ProfileImage" Width="200"/>
                </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Spacing="15">
                    <TextBlock Text="Username:" FontSize="16" Foreground="#02367B"/>
                    <TextBlock x:Name="UsernameTextBlock" FontSize="18"/>
                </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Spacing="15">
                    <TextBlock Text="Description:" FontSize="16" Foreground="#02367B"/>
                    <TextBlock x:Name="ProfileDescription" FontSize="16"/>
                </StackPanel>
            </StackPanel>

            <!-- Stocks List -->
        <StackPanel BorderThickness="4" BorderBrush="#02367B" Spacing="20" Padding="20" Background="#8fd3fe" CornerRadius="10">
            <TextBlock x:Name="UsernameMyStocks"  FontSize="20" FontWeight="Bold" Foreground="#02367B" />
            <ListView x:Name="StocksListView" SelectionMode="Single" BorderThickness="1" BorderBrush="#02367B" CornerRadius="10">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" Padding="10" FontSize="14" Foreground="#02367B"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="20">
                <Button Name="EnterStockButton" Content="Enter" CornerRadius="10" Click="getSelectedStock" Padding="5" Background="#02367B" Foreground="White" />
            </StackPanel>
        </StackPanel>

            <!-- Selected Stock Name -->
            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="20">
                <TextBlock x:Name="StockName" FontSize="18" FontWeight="SemiBold" Foreground="#02367B"/>
            </StackPanel>
        </StackPanel>
</Page>
