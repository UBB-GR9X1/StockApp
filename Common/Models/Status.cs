namespace Common.Models
{
    /// <summary>
    /// Defines the possible review statuses for a news article.
    /// </summary>
    public enum Status
    {
        Pending = 0,
        Approved = 1,
        Rejected = 2,
        All = 3,
    }
}
